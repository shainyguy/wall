# 🚀 Стена Подписчиков @medvedev.tech

Интерактивная "живая стена" всех подписчиков Instagram.

## 📁 Структура файлов

```
├── index.html       # Главный файл (всё в одном)
├── followers.json   # Список подписчиков
└── README.md        # Эта инструкция
```

## 🚀 Быстрый старт

1. Залей файлы на GitHub Pages
2. Подписчики могут сами добавляться через кнопку "Добавить себя"
3. Или добавляй вручную в `followers.json`

---

## 📝 Как добавлять подписчиков вручную

### Формат файла `followers.json`:

```json
{
  "meta": {
    "username": "medvedev.tech",
    "displayName": "Medvedev Tech",
    "lastUpdated": "2025-01-15",
    "instagramUrl": "https://instagram.com/medvedev.tech"
  },
  "followers": [
    {
      "id": 1,
      "username": "ivan_developer",
      "displayName": "Иван",
      "avatarUrl": "https://example.com/avatar.jpg",
      "addedAt": "2025-01-15",
      "isNew": true
    },
    {
      "id": 2,
      "username": "anna_design",
      "displayName": "Анна",
      "avatarUrl": "",
      "addedAt": "2025-01-14",
      "isNew": false
    }
  ]
}
```

### Поля подписчика:

| Поле | Обязательное | Описание |
|------|--------------|----------|
| `id` | ✅ | Уникальный номер (любое число) |
| `username` | ✅ | Instagram ник (без @) |
| `displayName` | ❌ | Имя для отображения |
| `avatarUrl` | ❌ | Ссылка на аватарку (если пусто — автогенерация) |
| `addedAt` | ❌ | Дата добавления (YYYY-MM-DD) |
| `isNew` | ❌ | `true` = показывать бейдж "NEW" |

---

## 🖼️ Аватарки

### Вариант 1: Автоматические
Оставь `avatarUrl` пустым — система создаст красивый градиентный аватар с первой буквой ника.

### Вариант 2: Ссылка на изображение
```json
"avatarUrl": "https://i.imgur.com/xxxxx.jpg"
```

### Где взять аватарки:
1. **Imgur** — загрузи фото на imgur.com, скопируй прямую ссылку
2. **GitHub** — положи в репозиторий: `avatars/username.jpg`
3. **Любой хостинг изображений**

### Вариант 3: Локальные аватарки
Создай папку `avatars/` и клади туда файлы:
```json
"avatarUrl": "avatars/username.jpg"
```

---

## 🔄 Автоматическое добавление

### Через сайт (самозапись)
Подписчики могут сами добавить себя:
1. Нажать "Добавить себя"
2. Ввести ник
3. Готово! (сохраняется в localStorage браузера)

### Экспорт подписчиков из Instagram
К сожалению, Instagram API не позволяет получать список подписчиков напрямую без одобренного приложения.

**Альтернативы:**

1. **Вручную** — добавляй подписчиков в JSON по мере подписки

2. **Phantombuster / IGExport** — платные сервисы для экспорта
   - Экспортируй в CSV
   - Конвертируй в JSON (см. скрипт ниже)

3. **Форма Google** — создай форму, куда подписчики сами вводят ник
   - Экспортируй в JSON

---

## 🛠️ Скрипт конвертации CSV → JSON

Если у тебя есть CSV файл с никами (по одному на строку):

```javascript
// convert.js (запусти в Node.js)
const fs = require('fs');

const csv = fs.readFileSync('followers.csv', 'utf8');
const usernames = csv.split('\n').map(u => u.trim()).filter(u => u);

const followers = usernames.map((username, index) => ({
  id: index + 1,
  username: username.replace('@', ''),
  displayName: '',
  avatarUrl: '',
  addedAt: new Date().toISOString().split('T')[0],
  isNew: index < 10 // первые 10 = новые
}));

const data = {
  meta: {
    username: 'medvedev.tech',
    displayName: 'Medvedev Tech',
    lastUpdated: new Date().toISOString(),
    instagramUrl: 'https://instagram.com/medvedev.tech'
  },
  followers
};

fs.writeFileSync('followers.json', JSON.stringify(data, null, 2));
console.log(`✅ Конвертировано ${followers.length} подписчиков!`);
```

---

## ✨ Функции сайта

- 🔍 **Поиск** — найди себя на стене
- 🎮 **Игра** — найди подписчика за 5 секунд
- 📱 **Mobile-first** — отлично работает на телефоне
- 🌟 **Анимации** — плавные эффекты и частицы
- ➕ **Самозапись** — подписчики добавляются сами
- 🔗 **Клик по карточке** — открывает профиль в Instagram

---

## 🎨 Кастомизация

В начале `<script>` в index.html:

```javascript
const CONFIG = {
    INSTAGRAM_URL: 'https://instagram.com/medvedev.tech',  // Твой профиль
    USERNAME: 'medvedev.tech',                              // Твой ник
    PARTICLE_COUNT: 40,                                     // Кол-во частиц
    HIGHLIGHT_INTERVAL: 4000,                               // Интервал подсветки (мс)
    GAME_DURATION: 5000,                                    // Время на игру (мс)
    // ...
};
```

---

## 📱 Деплой на GitHub Pages

1. Создай репозиторий (например `followers-wall`)
2. Загрузи все файлы
3. Settings → Pages → Source: Deploy from branch → main
4. Подожди 1-2 минуты
5. Готово! `https://username.github.io/followers-wall/`

---

## 💡 Советы

1. **Первые подписчики** — добавь 10-20 человек вручную для старта
2. **Промоутируй** — расскажи в Stories что есть стена
3. **Обновляй** — добавляй новых периодически
4. **Бейджи NEW** — ставь `isNew: true` для свежих подписчиков

---

Made with 💜 for @medvedev.tech
